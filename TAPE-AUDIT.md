# TAPE-AUDIT — hexa-earth

## A. Audit-class ledgers

- `state/markers/*.marker` — **10 markers** (`closure_consistency_*`, `lattice_arithmetic_*`, `real_limits_anchor_*`, `run_all_*`). Same four-verb pattern as hexa-cosmos/scope. **Class-T** fit.
- No JSONL, no audit subdir.

## B. Identity surface

`AGENTS.md` + `LATTICE_POLICY.md` + `IMPORTED_FROM_CANON.md`. Identity = Earth-system substrate. Fit for `hexa-earth/identity.tape` pinning planetary axes across the climate/geology/aviation/earthquake/desal sub-systems.

## C. Domain.md files

Only 8 root MD files. **Subdir-heavy** structure: `architecture/`, `aviation/`, `carbon_capture/`, `cartography/`, `civil-engineering/`, `climate/`, `desal/`, `desalination/` (dup), `earth_defense/`, `earthquake/`, `env_protect/`, `env_thermal/`, `fire-science/`, `forensic-science/`, `geology/`, `hexa_defense/`, `hexa-exo/`, `hexa-skyway/`, `hexa-tsunami/`, `hexa-weather/`. Each subdir is a domain-promotion candidate (current ratio 8 MDs : ~20 subdirs is upside-down per governance #4). `desal/` + `desalination/` are duplicate — cleanup candidate.

## D. Per-run/per-event history

Same four-verb run vocabulary as hexa-cosmos. Sparse but uniform — easy migration target.

## E. Promotion candidates

- **n6 atoms** — orbital-mechanics constants (g, GM_Earth), atmospheric composition, plate-tectonic timescales. Forensic-science evidentiary-class constants.
- **hxc binaries** — satellite imagery, seismograph traces, weather radar grids.
- **n12 cube cells** — sphere (litho/hydro/atmo/bio/cryo/anthropo) × scale × duty.

## Verdict

**LIGHT** — 10 markers, but **subdir-to-MD ratio is inverted** (largest gap in the batch). Tape role secondary; bigger immediate win is the domain-promotion sweep (subdir → root MD).
